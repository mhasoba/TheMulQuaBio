#include <stdio.h>

int main(void)
{
    int x;
    /* This is a block comment
     * I can have as many lines as 
     * I want. */
    printf ("Hello, World!\n"); // This is an inline comment
    printf("x + 1 is: %i\n", x + 1);
    return 0;
}
